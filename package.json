{
  "name": "max-moderation-bot",
  "version": "1.0.0",
  "description": "MAX moderation bot with anti-link, anti-spam and daily quota controls",
  "private": true,
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx src/index.ts",
    "start": "node dist/index.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "local:push": "bash scripts/local-commit-push.sh",
    "vps:deploy": "bash scripts/vps-deploy.sh",
    "commands:install": "bash scripts/install-global-commands.sh",
    "members:add": "node scripts/add-chat-members-2026.js"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "@maxhub/max-bot-api": "^0.2.2",
    "better-sqlite3": "^12.4.1",
    "dotenv": "^17.2.3"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/node": "^24.4.0",
    "tsx": "^4.20.5",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
